fn reset<T>(&mut T: String, &mut only_space: String, &mut comment_line: String,
    &mut prev_char: String)
where T: Display,
{
    println!("{}{}{}{}", &line, &only_space, &comment_line, &prev_char);
}
